apiVersion: apps/v1
kind: Deployment
metadata:
  name: lora-swap-deployment
  labels:
    app: lora-swap
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lora-swap
  template:
    metadata:
      labels:
        app: lora-swap
        snapshot-agent: "true"
        timeslice.io/job-id: "lora-test-5"
    spec:
      hostIPC: true
      hostPID: true
      containers:
      - name: lora-swap
        image: asia-southeast1-docker.pkg.dev/edwinhernandez-gke-dev/snapshot-example/lora-swap-client:latest # Replace with your actual image tag
        command: ["python3"]
        args: ["test_lora_swap.py"]
        securityContext:
          runAsUser: 0
        imagePullPolicy: Always
        env:
        - name: NODE_IP
          valueFrom:
            fieldRef:
              fieldPath: status.hostIP
        - name: AGENT_ENDPOINT
          value: "$(NODE_IP):9001"
        - name: JOB_ID
          value: "lora-test-5"
        - name: GROUP
          value: "lora-group"
        - name: GPU_VENDOR
          value: "NVIDIA"
        - name: GPUCR_NUM_THREADS
          value: "16"
        - name: GPUCR_SHM_GB
          value: "60"
        - name: GPUCR_STAGING_MB
          value: "1024"
        - name: VLLM_ALLOW_INSECURE_SERIALIZATION
          value: "1"
        - name: LD_PRELOAD
          value: "/usr/local/lib/vGPU-NVIDIA.so"
        - name: VLLM_ENABLE_V1_MULTIPROCESSING
          value: "0"
        - name: LD_LIBRARY_PATH
          value: "/usr/local/cuda/lib64"
        - name: PYTORCH_NO_CUDA_MEMORY_CACHING
          value: "1"
        - name: CUDA_VISIBLE_DEVICES
          value: "0"
        - name: CUDA_LAUNCH_BLOCKING
          value: "1"
        volumeMounts:
        - name: huge-ckpt
          mountPath: /mnt/huge-ckpt
        resources:
          claims:
            - name: gpus-for-pod
      volumes:
      - name: huge-ckpt
        hostPath:
          path: /var/tmp/huge-ckpt
          type: DirectoryOrCreate
      resourceClaims:
      - name: gpus-for-pod
        resourceClaimName: shared-two-gpus-claim
      tolerations:
      - key: "nvidia.com/gpu"
        operator: "Exists"
        effect: "NoSchedule"
