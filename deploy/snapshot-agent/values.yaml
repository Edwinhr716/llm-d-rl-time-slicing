# Default values for snapshot-agent.
image:
  repository: ghcr.io/llm-d-incubation/llm-d-rl-time-slicing/snapshot-agent
  pullPolicy: Always
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

podAnnotations: {}

resources: {}
#  limits:
#    cpu: 500m
#    memory: 1Gi
#  requests:
#    cpu: 100m
#    memory: 256Mi

nodeSelector: {}

tolerations:
  - key: "nvidia.com/gpu"
    operator: "Equal"
    value: "present"
    effect: "NoSchedule"

affinity: {}

port: 9001

securityContext:
  privileged: true

nvidia:
  driver:
    enabled: true
    hostPath: /home/kubernetes/bin/nvidia
    # Path inside the container where the driver will be mounted
    mountPath: /usr/local/nvidia
    # List of library search paths inside the container
    libraryPaths:
      - /usr/local/nvidia/lib64
  devices:
    enabled: true
    hostPath: /dev
    mountPath: /dev

# GPU-CR memory-regions backend machinery. Default disabled: the chart is
# identical to a CUDA/app-backend deployment unless this is turned on.
memoryRegions:
  enabled: false
  # Shared checkpoint/control dir (EXPORT_FILE_PATH inside the agent).
  ctlDir: /mnt/huge-ckpt
  # Host path backing ctlDir; also the hugetlbfs mountpoint.
  hostCtlPath: /var/tmp/huge-ckpt
  hugetlbfs:
    mount: true # run the nsenter init container
    pagesize: 2M
  # hugetlb cgroup headroom for mmap-write restores into live dump buffers.
  # Rendered as hugepages-<pagesize>i requests/limits (k8s requires a memory
  # request alongside hugepages; one is added if resources doesn't set one).
  hugepagesResource: 2Gi
  # Snapshot store. Must NOT live on hugetlbfs (no write(2); copies there
  # would pin hugepages forever). Default is node tmpfs: dumps are multi-GB
  # and copied on every swap, so a disk-backed store dominates swap latency
  # (measured 2026-08-11: ~12s write / ~8.4s read per ~2GB dump on a GKE
  # boot disk vs ~2s round-trip on tmpfs, against a 0.3s GPU-CR op).
  # NOTE: tmpfs writes are charged to the agent's memory cgroup and are not
  # reclaimable — size resources.limits.memory to hold the largest
  # concurrent dump set (see the daemonset template fallback), or the agent
  # is OOMKilled mid-copy, stranding the workload mid-checkpoint.
  snapshotDir: /var/snapshots
  snapshotsHostPath: /dev/shm/gcr-snapshots
  copyHostFile: false # GPU_CR_COPY_HOST_FILE
  opTimeoutSec: 120 # GPU_CR_OP_TIMEOUT_SEC

rbac:
  create: true

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""